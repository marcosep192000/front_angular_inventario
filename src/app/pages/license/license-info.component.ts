import { CommonModule } from '@angular/common';import { Component, OnInit } from '@angular/core';import { MatButtonModule } from '@angular/material/button';import { MatIconModule } from '@angular/material/icon';import { RouterLink } from '@angular/router';import { finalize } from 'rxjs';import { LicenseStatus, licenseStatusMessage } from '../../interfaces/license';import { LicenseService } from '../../services/license.service';
@Component({selector:'app-license-info',standalone:true,imports:[CommonModule,MatButtonModule,MatIconModule,RouterLink],templateUrl:'./license-info.component.html',styleUrl:'./license-info.component.css'})
export class LicenseInfoComponent implements OnInit { status:LicenseStatus|null=null;loading=true;error='';constructor(private api:LicenseService){}ngOnInit(){this.refresh();}refresh(){this.loading=true;this.error='';this.api.obtenerEstado(true).pipe(finalize(()=>this.loading=false)).subscribe({next:s=>this.status=s,error:()=>this.error='No se pudo conectar con Inventario Pixel. Verificá que el servidor esté funcionando.'});}message(){return this.status?licenseStatusMessage(this.status.status):'';}limit(current:number,max:number){return max===-1?`${current} / Ilimitados`:`${current} de ${max.toLocaleString('es-AR')}`;}percent(current:number,max:number){return max<=0?0:Math.min(100,current/max*100);}type(){return this.status?.licenseType==='PERMANENT'?'Permanente':'Temporal';}}
