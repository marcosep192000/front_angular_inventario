import{FormBuilder}from'@angular/forms';import{of,throwError}from'rxjs';import{CreatePriceListDialogComponent}from'./create-price-list-dialog.component';

describe('CreatePriceListDialogComponent',()=>{let api:any,toast:any,ref:any,component:CreatePriceListDialogComponent;beforeEach(()=>{api={createPriceList:jasmine.createSpy('createPriceList')};toast={success:jasmine.createSpy('success')};ref={close:jasmine.createSpy('close')};component=new CreatePriceListDialogComponent({suppliers:[]},new FormBuilder(),api,toast,ref);});
it('submits the exact backend contract once, trimming the name and preserving local dates',()=>{api.createPriceList.and.returnValue(of({list:{id:15}}));component.form.setValue({providerId:2,name:'  WW  ',referenceDate:'2026-09-06',validFrom:'2023-06-02',validUntil:'2026-09-02',notes:' D '});component.save();expect(api.createPriceList).toHaveBeenCalledTimes(1);expect(api.createPriceList).toHaveBeenCalledWith({providerId:2,name:'WW',referenceDate:'2026-09-06',validFrom:'2023-06-02',validUntil:'2026-09-02',sourceFileName:null,notes:'D'});});
it('blocks an invalid date range before calling the service',()=>{component.form.setValue({providerId:2,name:'WW',referenceDate:'2026-09-06',validFrom:'2026-09-03',validUntil:'2026-09-02',notes:''});component.save();expect(api.createPriceList).not.toHaveBeenCalled();expect(component.error).toContain("Válida desde");});
it('requires reference date and rejects a whitespace-only name',()=>{component.form.patchValue({providerId:2,name:'   ',referenceDate:''});component.save();expect(component.form.invalid).toBeTrue();expect(api.createPriceList).not.toHaveBeenCalled();});
it('shows useful backend errors and releases the saving guard',()=>{api.createPriceList.and.returnValue(throwError(()=>({status:403})));component.form.setValue({providerId:2,name:'WW',referenceDate:'2026-09-06',validFrom:'',validUntil:'',notes:''});component.save();expect(component.error).toContain('permisos');expect(component.saving).toBeFalse();});});
