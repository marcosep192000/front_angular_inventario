# Reparacion

Ejecute el instalador con `-Repair`. Recrea/refresca WinSW, repara reglas propias, ejecuta el setup idempotente del scanner y valida healthchecks. Preserva `application-prod.properties`, DB y CA; ante cambio de IP solo se renueva el certificado servidor.
